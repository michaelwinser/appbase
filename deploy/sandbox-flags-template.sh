# Project-specific nono sandbox capabilities.
# Sourced by ./sandbox — edit this file, not ./sandbox.
#
#   --allow <path>         Read-write access
#   --read <path>          Read-only access
#   --allow-bind <port>    Allow binding to a port

# APP_FLAGS="$APP_FLAGS --allow $HOME/.config/myapp"
# APP_FLAGS="$APP_FLAGS --allow-bind 3000"
